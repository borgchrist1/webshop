@model List<webshop.Models.MerchandiseModel>

@{
    ViewBag.Title = "Home Page";
    var cart = Session["Cart"];

}
<div class="container">
    @*<div class="row">
        <div class="jumbotron">
            <h1>ASP.NET</h1>
            <p class="lead">ASP.NET is a free web framework for building great Web sites and Web applications using HTML, CSS and JavaScript.</p>
            <p><a href="http://asp.net" class="btn btn-primary btn-lg">Learn more &raquo;</a></p>
        </div>
    </div>*@
    

    @foreach (var item in Model)
    {
        <div class="col-xs-12 col-sm-6 col-md-3">
            <!-- PROD. ITEM -->
            <div class="span4">
                <div class="thumbnail">
                    <!-- IMAGE CONTAINER-->
                    <img src="@Url.Content("/Content/img/" + item.Productimg)" alt="post image">
                    <!--END IMAGE CONTAINER-->
                    <!-- CAPTION -->
                    <div class="caption">
                        <h3 class=""><a href="Home/Details/@(item.Id)">@item.Name</a></h3>
                        <p class="">@item.Dscription</p>

                        <div class="row-fluid">
                            <div class="span6">
                                <p class="lead">@item.Price Kr</p>
                            </div>
                            <div class="span6">
                                <a class="btn btn-success btn-block add" href="#" data-set="@(item.Id)">Add to cart</a>
                            </div>
                        </div>
                    </div>
                    <!--END CAPTION -->
                </div>
                <!-- END: THUMBNAIL -->
            </div>
            <!-- PROD. ITEM -->
        </div>
    }

</div><!--end container-->